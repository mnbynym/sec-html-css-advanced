# 応用編で学ぶ HTML / CSS スキルのまとめ

MDN Blog 風ページの発展（Sample A）とお問い合わせフォーム（Sample B）で必要なスキルの一覧。

| サンプル / ステップ | テーマ | 主なスキル |
| --- | --- | --- |
| A / step1 | Modern CSS リファクタ | ネスト・`clamp()`・`color-mix()`・`:has()` |
| A / step2 | 高度なレイアウト | sticky・RAM パターン・subgrid・コンテナクエリ |
| A / step3 | アニメーション | `@keyframes`・スクロール連動・View Transitions |
| B / step1 | フォームのマークアップ | フォーム部品・`label`・制約属性 |
| B / step2 | バリデーション UX | `:user-invalid`・`accent-color`・`:focus-visible` |

---

## Sample A / Step 1 — Modern CSS へのリファクタリング

| スキル | 説明 |
| --- | --- |
| CSS ネスト | コンポーネント単位でルールをまとめる。`&:hover` で擬似クラスも内側に書ける |
| ネストした `@media` | メディアクエリを対象コンポーネントの中に書き、末尾の「レスポンシブまとめ書き」をなくす |
| `clamp(最小, 推奨, 最大)` | 流体タイポグラフィ。`rem + vw` の推奨値で画面幅に比例して伸縮 |
| `color-mix()` | 基本色からホバー色を自動生成。トークンの数を減らせる |
| `:has()` | 子や後続の状態から**親側**を選択できる唯一のセレクタ |
| リファクタリングの姿勢 | 見た目を変えずに書き直し、before / after を見比べて検証する |

## Sample A / Step 2 — 高度なレイアウト

| スキル | 説明 |
| --- | --- |
| `position: sticky` + `z-index` | スクロール追従ヘッダー。重なり順の管理 |
| RAM パターン | `repeat(auto-fit, minmax(280px, 1fr))`。メディアクエリなしで列数が変わる |
| サブグリッド | `grid-template-rows: subgrid` で親の行トラックを借り、カード同士の行の高さを揃える |
| コンテナクエリ | `container-type: inline-size` + `@container`。ビューポートでなく**部品の幅**で分岐 |
| 使い分けの判断 | ページの大枠はメディアクエリ、再利用部品はコンテナクエリ |

## Sample A / Step 3 — アニメーションとインタラクション

| スキル | 説明 |
| --- | --- |
| `translate` / `scale` | `transform` の個別プロパティ。ホバーの浮き上がりなどに使う |
| `box-shadow` | 影。浮き上がり演出とセットで |
| `@keyframes` + `animation` | 表示時のフェードインなど、多段階の動きの定義 |
| スクロール連動 | `animation-timeline: view()`。時間でなくスクロール位置で進行（対応ブラウザ限定） |
| `@supports` | 未対応ブラウザで壊れないようにするプログレッシブ・エンハンスメント |
| View Transitions | `@view-transition { navigation: auto }` で MPA のページ遷移をアニメーション化 |
| `view-transition-name` | 2 ページの要素を「同一のもの」として繋ぎ、モーフィングさせる |
| `prefers-reduced-motion` | 「動きを減らす」設定のユーザーにはアニメーションを止める配慮 |

## Sample B / Step 1 — Web フォームのマークアップ

| スキル | 説明 |
| --- | --- |
| `<form>` / `<button type="submit">` | フォームの骨格と送信 |
| `<label>` の関連付け | `for` + `id`、またはラベルで部品を包む。アクセシビリティの基本 |
| `<input type="...">` | `text` / `email` など。type だけで形式チェックとキーボード切替が働く |
| `<select>` / `<option>` | 選択式。先頭に `value=""` の案内項目を置くと `required` が機能する |
| `<fieldset>` / `<legend>` | ラジオボタンなど関連する選択肢のグループ化 |
| `<textarea>` | 複数行入力。`resize: vertical` で挙動を制御 |
| 制約属性 | `required` / `minlength` / `placeholder` / `autocomplete`。JS なしの検証の土台 |
| フォーム部品のスタイリング | リセット CSS 後の入力欄に枠線・余白・角丸を自分で設計する |

## Sample B / Step 2 — CSS だけのバリデーション UX

| スキル | 説明 |
| --- | --- |
| `:user-invalid` / `:user-valid` | 「ユーザーが操作した後」だけ反応する検証擬似クラス。`:invalid` との違いが最重要 |
| `:has()` の実用 | 必須欄を持つ行のラベルに `::after` で `*` を自動表示 |
| エラーの出し分け | `:user-invalid ~ .hint` で兄弟のメッセージを表示 |
| `accent-color` | チェックボックス・ラジオのテーマカラーを 1 行で指定 |
| `:focus-visible` | キーボード操作時だけフォーカスリングを出す |
| `form:invalid` | フォーム全体の状態で送信ボタンの見た目を変える（無効化は CSS ではできない） |

---

## 応用編でも扱わないスキル（次回以降）

- JavaScript（DOM 操作・イベント・`fetch` による Web API の利用・エラー文言のカスタマイズ）
- ダークモード対応（`prefers-color-scheme`）
- `<picture>` / `srcset` によるレスポンシブ画像
- `@property` によるカスタムプロパティの型付け
